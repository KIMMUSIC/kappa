제안해 준 Phase 2 설계안과 LangGraph V2 흐름도, 파서 라우팅 전략을 꼼꼼히 확인했다.

특히 `<action>`과 `<tool_call>`을 상호 배타적으로 분리하여 에이전트의 혼란을 원천 차단한 점과, 상태(State)의 기본값을 우아하게 처리해 기존 47개 테스트의 회귀(Regression)를 완벽히 방어한 설계가 대단히 탁월하다. 설계안을 100% 승인(Approve)한다!

[작업 실행 지시사항]
Phase 1 때와 마찬가지로 한 번에 모든 것을 다 짜지 말고, 인프라 기반부터 엄격한 TDD 방식으로 아래 순서대로 진행해라.

1. 먼저 **[Task 1: 가상 파일 시스템(VFS) 기반 장기 메모리]** 관련 코드만 우선 작성해라. (Task 2, 3, 4나 LangGraph 노드는 아직 절대 건드리지 마라)
   - `src/kappa/config.py` 에 `MemoryConfig` 추가
   - `src/kappa/memory/vfs.py` 작성 (보안을 위해 `../` 등의 경로 이탈(Directory Traversal) 공격 방지 로직 필수 구현)

2. Task 1 코드를 작성했다면, 테스트 코드(`tests/test_vfs.py`)를 작성하고 스스로 실행하여 검증해라.
   - VFS 격리망이 잘 작동하여 외부 폴더로 접근 시도 시 `ValueError` 등의 예외가 확실하게 발생하는지 검증할 것.
   - 지정된 워크스페이스 내에서의 정상적인 읽기, 쓰기, 파일 목록 조회가 잘 동작하는지 검증할 것.

3. **[핵심 검증]:** `pytest`를 실행하여 새로 만든 VFS 테스트뿐만 아니라, **Phase 1의 기존 47개 테스트가 단 하나도 깨지지 않고 100% 통과(All Green)하는지 반드시 확인**해라.

테스트 결과가 모두 완벽하게 통과하면 그 결과(Test Summary)를 나에게 브리핑해라. 결과를 확인한 뒤 Task 2(Tool Registry) 진행을 승인하겠다. 즉시 Task 1 구현을 시작해라!