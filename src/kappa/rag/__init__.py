"""Domain-specific RAG (Retrieval-Augmented Generation) pipeline.

Provides document ingestion (chunking + embedding + indexing) and
a ``KnowledgeSearchTool`` that plugs into the ``ToolRegistry``.
"""
